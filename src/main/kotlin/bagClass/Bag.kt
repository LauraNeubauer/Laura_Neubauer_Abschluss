package bagClass

// Klasse des Beutels mit Amount welcher ausschlagebend ist ob der Beutel im Menü angezeigt wird oder nicht
class Bag(
    var name: String,
    var value: Int,
    var amount: Int
) {
}